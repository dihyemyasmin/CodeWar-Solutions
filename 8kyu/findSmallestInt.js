/*Given an array of integers your solution should find the smallest integer.

The solution is 


The take away is don't forget Math.min to attain the minimal number and "..." tells the function to take all the numbers from the array.*/

function findSmallestInt(arr) {
 
    return Math.min(...arr);
   }